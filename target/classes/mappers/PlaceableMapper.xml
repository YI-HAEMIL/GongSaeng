<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.PlaceableMapper">
	<select id="selectList" resultType="vo.PlaceableVO">
		SELECT * FROM place_able
		WHERE bizm_id=#{bizm_id}
	</select>
	<insert id="insert">
		INSERT INTO place_able VALUES(
		#{place_id},#{bizm_id},#{able_date},#{able_starttime},#{able_endtime})
	</insert>
	<delete id="delete">
		DELETE FROM place_able WHERE able_date=#{able_date}
	</delete>
</mapper>